{
  "name": "pre-commit-folder-enforcer",
  "version": "1.2.0",
  "description": "Pre-commit hook enforcing folder-based commit rules with auto-prefix",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "prepare": "husky",
    "precommit": "node dist/cli.js"
  },
  "bin": {
    "precommit": "./dist/cli.js"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "husky": "^9.0.11",
    "simple-git": "^3.25.0",
    "typescript": "^5.5.4"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
